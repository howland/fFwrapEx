Compilation:

First go to  lapack_dependency
and execute cmpllpack

Then copy the lapack.a into the
src_<appname> directories and execute the compile
scripts.  


Test in test_inps:

./<appname>.exe inp4 <outname>

Standard output in <outname>
